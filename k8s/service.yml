apiVersion: v1
kind: Service
metadata:
  name: ml-sentiment-service
spec:
  selector:
    app: ml-sentiment-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7860
  type: ClusterIP